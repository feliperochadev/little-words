appId: com.littlewords.app
name: "Export - Share and save CSV export"
---

- extendedWaitUntil:
    visible: "Settings"
    timeout: 15000

# Navigate to Settings tab
- tapOn: "Settings"
- waitForAnimationToEnd

# ============================
# EXPORT - SHARE CSV
# ============================

# Scroll to export section
- scrollUntilVisible:
    element:
      id: "settings-export-title"
    direction: DOWN
- assertVisible:
    id: "settings-export-title"

# Tap Share button
- tapOn:
    id: "settings-share-btn"
- waitForAnimationToEnd

# The share sheet will appear (OS-level) - dismiss it
# On Android, press back to dismiss
- back
- waitForAnimationToEnd

# ============================
# EXPORT - SAVE TO DEVICE
# ============================

# Tap Save button
- tapOn:
    id: "settings-save-btn"
- waitForAnimationToEnd

# A file picker / save dialog may appear (OS-level) - dismiss it
- back
- waitForAnimationToEnd
